---
description: 全项目链上写交易须排队；单钱包 FIFO 串行，多签字钱包可用 push/shift 钱包池并行提速
alwaysApply: true
---

# 链上写交易：排队系统（全局）

凡 **发送链上交易**（`sendTransaction`、`contract.write`、`tx.wait()` 直至 receipt 确认，含代付 gas relay），**必须**通过 **排队系统** 执行，**禁止**对同一签名者（EOA / relayer 热钱包）并发送 tx。

排队系统分 **两层**，可组合使用：

| 层 | 目的 | 语义 |
|---|---|---|
| **① 业务任务队列** | 有序处理业务 | `push` 入队 → `shift` 取任务（FIFO） |
| **② 签名者维度** | 隔离 nonce | **单钱包** 串行车道；**多钱包** 可并行 |

**提速原则**：持有 **多个** 可签字热钱包时，用 **钱包池 `push`/`shift`/`unshift`** 把并行度提高到「钱包数量」；**不得**用多钱包绕过业务顺序或双持同一钱包。

与 **`beamio-api-cluster-master.mdc`**（Master `*Pool` + `Settle_ContractPool`）为同一模型的服务端标准实现。

---

## 核心规则

### A. 单签字钱包 → 单车道 FIFO（必须串行）

1. **入队**：链上写须包在 **车道队列** 任务内（含 `await tx.wait()`）。
2. **FIFO**：同一车道任意时刻 **最多一个 in-flight** 写。
3. **出队**：任务开始执行时 `shift` 移出等待队列；**成功或失败** 后任务结束。
4. **车道 key** = 签名者：`onchainTxLaneForSigner(eoa)` → `eoa:0x…`；单热钱包 daemon 可用进程级常量（如 `CONET_VALIDATOR_NODE_ONCHAIN_LANE`）。
5. **禁止** 同 signer 上 `void sendTx()`、`Promise.all` 多笔写、live + backfill 并发抢 nonce。

### B. 多签字钱包 → 钱包池 `push`/`shift` 并行（推荐提速）

当服务配置 **≥2 个** 独立签字 EOA（各自 nonce 链）时：

1. **钱包池**：`WalletPool: Signer[]`，启动时 `push` 填入全部可用签字钱包（或从 `Settle_ContractPool` 初始化）。
2. **借钱包**：每个写任务 **`const sc = WalletPool.shift()`**；池空则 **延迟重试**（`setTimeout` 链），**禁止**忙等或双持。
3. **还钱包**：`try/finally` 内 **`WalletPool.unshift(sc)`**，无论 tx 成功或失败都必须归还。
4. **并行度**：不同任务占用 **不同** `sc` 时可 **同时** `sendTransaction`；**同一 `sc` 仍须串行**（一个 worker 持有时不得第二笔写）。
5. **业务池 + 钱包池**（Master 标准）：
   - 业务 `requestPool.push(task)` → `kickPoolPress()` 可启多个 worker；
   - 每 worker：`shift` 一条业务任务 + `shift` 一个钱包 → 写链 → `finally` 还钱包 → `schedule` 下一条。
6. **不得** 为并行把 **同一私钥** 注册进池两次；不得 `shift` 后 early-return 路径漏 `unshift`。

```typescript
// 多钱包并行模板（与 Settle_ContractPool / *PoolPress 一致）
export async function examplePoolPress() {
  const task = businessPool.shift()
  if (!task) return
  const sc = signerPool.shift()
  if (!sc) {
    businessPool.unshift(task)
    return setTimeout(() => kickPoolPress(), 3000)
  }
  try {
    const tx = await contract.write(sc.wallet)
    await tx.wait()
    task.resolve(tx.hash)
  } catch (e) {
    task.reject(e)
  } finally {
    signerPool.unshift(sc) // 必须归还
    schedulePoolPress()
  }
}

function kickPoolPress() {
  // 每个空闲钱包可各跑一个 worker → 多笔 tx 并行
  while (signerPool.length > 0 && businessPool.length > 0) {
    examplePoolPress().catch(log)
  }
}
```

### C. 何时用哪种

| 条件 | 做法 |
|---|---|
| 仅 **1** 个热钱包 | `enqueueOnchainTxWork(lane, …)` 单车道 FIFO |
| **≥2** 个热钱包、吞吐优先 | 业务 `push`/`shift` + 钱包池 `shift`/`unshift` 并行 |
| 共用本地资源（`validator_deposits.json`、Prysm import） | 资源敏感段仍可用 **单进程车道** 串行；仅 **纯链上写** 段用多钱包池 |
| 客户端用户 EOA | 钱包自管 nonce；**服务端代付** 仍须 ① 或 ② |

---

## 适用场景

| 场景 | 排队方式 |
|---|---|
| **x402sdk Master** | 业务 `*Pool` **`push`/`shift`** + **`Settle_ContractPool` `shift`/`unshift`**（多钱包并行）— 见 `beamio-api-cluster-master.mdc` |
| **Validator listener（单 redeem-admin）** | `enqueueOnchainTxWork(CONET_VALIDATOR_NODE_ONCHAIN_LANE, …)` 单车道；若增第二签字钱包须改 **per-signer lane** 或钱包池 |
| **定时 daemon 扫链写合约** | 写任务入队；多钱包时用池并行；tick 须 `beamio-interval-daemon-no-overlap.mdc` |
| **客户端 PWA / bizSite** | 用户 EOA 由钱包管 nonce；服务端代付走 Master 钱包池 |
| **脚本 / 部署** | 单 deployer 串行；多 deployer 可各启 worker + 各钱包池 |

---

## 标准实现（x402sdk / Node）

**单一事实来源模块：** `src/x402sdk/src/onchainTxSerialQueue.ts`

```typescript
import {
  enqueueOnchainTxWork,
  waitForOnchainTxQueue,
  waitForAllOnchainTxQueues,
  CONET_VALIDATOR_NODE_ONCHAIN_LANE,
  onchainTxLaneForSigner,
} from '../onchainTxSerialQueue'

// 整段写操作（含 receipt）入队
await enqueueOnchainTxWork(
  onchainTxLaneForSigner(admin.address),
  'fundAndDepositValidators',
  async () => {
    const tx = await contract.fundAndDepositValidators!(...)
    await tx.wait()
  },
  '[myDaemon]'
)

// SIGTERM 前等待车道清空
await waitForOnchainTxQueue(CONET_VALIDATOR_NODE_ONCHAIN_LANE)
```

### 任务边界

- ✅ 入队函数内：`estimateGas`（可选）→ `send` / 合约写 → **`await tx.wait()`** → 依赖 receipt 的后续一步
- ✅ 共用本地资源（如 `validator_deposits.json`、Prysm import）的 **整段 claim 流水线** 可与 tx 同车道打包
- ❌ 仅预检 / `eth_call` / `staticCall` — **不入队**（只读无 nonce）
- ❌ 入队任务内再 `void` 第二个未入队的同 signer 写

### 日志（推荐）

- 入队：`tx queue +{label} lane={laneKey} waiting=N`
- 开始：`tx start {label} (queued Xms; N behind)`
- 关闭：`waitForOnchainTxQueue` 超时须打 `ALERT` 级日志

---

## 与 Master / 钱包池的关系

| 层 | 结构 | 并行 |
|---|---|---|
| **业务 pool** | `push(task)` / `shift()` | 可多 worker 同时 dequeue **不同** 任务 |
| **签字钱包池** | `Settle_ContractPool`：`shift(sc)` → 写链 → `finally { unshift(sc) }` | **钱包数 N ⇒ 最多 N 笔 tx 并行** |
| **单钱包车道** | `enqueueOnchainTxWork(lane, …)` | 并行度 1 |

新增 Master `*PoolPress`：**必须** 业务 pool + `Settle_ContractPool` shift/unshift；**不得**对同一 `sc` 并发两笔 tx。

扩容检查：增加 `beamio_Admins` / settle 钱包后，确认 pool 初始化 `push` 全部地址且 worker 在池非空时 `kick` 多路。

---

## 参考实现

| 模块 | 路径 |
|---|---|
| 通用车道队列 | `src/x402sdk/src/onchainTxSerialQueue.ts` |
| Validator claim / deposit | `src/x402sdk/src/endpoint/validatorDepositRedeem.ts` — `enqueueListenerOnchainWork` |
| CL skim payout | `src/x402sdk/src/endpoint/validatorClRewardPayoutReporter.ts` |
| Daemon 优雅关闭 | `src/x402sdk/src/endpoint/validatorDepositRedeemListenerDaemon.ts` — `waitForListenerSerialQueue` |
| Master 多钱包 pool | `src/x402sdk/src/MemberCard.ts` — `*PoolPress` + `Settle_ContractPool` |

---

## AI 检查清单

- [ ] 链上写是否入队（单车道 **或** 业务 pool + 钱包池）？
- [ ] **多钱包**场景是否 `shift` 借钱包、`finally` **`unshift` 归还**？
- [ ] 池空时是否 **delay 重试**，而非并发双持同一钱包？
- [ ] 同一 signer 是否无 `void` / `Promise.all` 并行写？
- [ ] 任务是否 **`await tx.wait()`** 后才算完成并释放钱包？
- [ ] live + backfill 是否会抢同一 nonce？
- [ ] SIGTERM 是否 `waitForOnchainTxQueue` / 等待业务池 + 在途 tx 结束？
- [ ] 增钱包后是否更新 pool 初始化与 `kick` 并行度？

---

## 严禁

- ❌ 同一热钱包并发写而不入队 / 不借还钱包池
- ❌ `shift` 钱包后 early-return **漏 `unshift`**
- ❌ 同一私钥在池内出现两次以「伪造并行」
- ❌ 仅用 `inFlight` Set 防重复 requestId，却允许多 claim 并发抢同一 nonce
- ❌ 队列任务 `shift` 后丢失 Promise（须 resolve/reject）
- ❌ 为「更快」跳过 `tx.wait()` 即还钱包、启下一任务
- ❌ `setInterval` 周期内裸送 tx（须 `setTimeout` 链 + 排队，见 `beamio-no-setinterval.mdc`）
- ❌ 有多钱包仍强制全局单车道，导致 **不必要** 吞吐瓶颈（资源互斥段除外）

## 相关守则

- `beamio-api-cluster-master.mdc` — Cluster 预检 + Master 业务 pool + Settle 多钱包
- `beamio-interval-daemon-no-overlap.mdc` — daemon tick 不与写队列混淆：tick 串行，写车道 FIFO
- `conet-validator-redeem-listener-l1-workflow.mdc` — validator append workflow
- `beamio-entrypoint-confirmed-return-protocol.mdc` — HTTP 边界在 receipt 后；队列内仍须 wait receipt
