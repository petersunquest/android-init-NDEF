---
description: Beamio beamio.app 站点根 rsync 保护（app/PWA 产物 + homepage/SilentPassUI 服务器 Git 克隆）
alwaysApply: true
---

# Beamio `beamio.app` 站点根：rsync `--delete` 保护（产物 + 服务器 Git 克隆）

## 核心规则

**对 `/var/www/beamio.app/` 做任何 `rsync --delete`（homepage 发布、手工 SSH、AI 自动化）时，必须携带下文「强制 exclude 清单」中的**全部**项。** 缺一项即可能删掉线上 PWA、POS、**服务器上的 homepage / SilentPassUI Git 工作目录**，或 **Universal Links**（`.well-known/`）。

**Homepage 部署只同步静态 build 产物**（`index.html`、`static/`、`assets/` 等），**不得**用 homepage 的 `build/` 去「同步整站根目录」而误删并行的其它目录。

## 服务器 `/var/www/beamio.app/` 目录语义

| 目录 | 类型 | 用途 |
|------|------|------|
| **`app/`** | **线上产物** | SilentPassUI PWA build（`https://beamio.app/app/`） |
| **`appTemp/`** | **staging 产物** | PWA 发布暂存，再 promote 到 `app/` |
| **`pos/`** | **线上产物** | POS PWA build |
| **`posTemp/`** | **staging 产物** | POS PWA 暂存 |
| **`homepage/`** | **服务器 Git 克隆** | `beamio-APP/homepage`，远程 `git pull` + `npm run build` 的工作目录；**不是** nginx 直接服务的静态根 |
| **`SilentPassUI/`** | **服务器 Git 克隆** | `CoNET-project/SilentPassUI`（`cashtree`），远程 build PWA 的工作目录；**不是** `app/` 本身 |
| **`.well-known/`** | **运维静态** | 如 `apple-app-site-association`；homepage build **通常不含**，无 exclude 会被 `--delete` 清掉 |

站点根上的 `index.html`、`static/`、`assets/` 等来自 **`src/homepage` build**，与上表目录**并列**；一次无 exclude 的 `rsync --delete build/ /var/www/beamio.app/` 会把表中目录当作「build 里不存在」而**删除**（含 `.git` 源码树）。

### 事故复盘（禁止重演）

2026-06-02：手工 `rsync -av --delete build/ /var/www/beamio.app/` **仅** exclude 了 `app/`、`appTemp/`、`pos/`、`posTemp/`，**漏掉** `homepage/`、`SilentPassUI/` → 服务器上两套 Git 克隆被删；`.well-known/` 亦被删。恢复方式：`git clone` 回 `homepage/`、`SilentPassUI/`，并从 `src/CashTrees_iOS/docs/apple-app-site-association.draft.json` 还原 AASA。

## 强制 exclude 清单（homepage → 站点根，单一事实来源）

与 `scripts/deployBeamioHomepage.sh` 中 `RSYNC_EXCLUDES` **必须一致**：

```bash
--exclude 'app/'
--exclude 'appTemp/'
--exclude 'pos/'
--exclude 'posTemp/'
--exclude 'homepage/'
--exclude 'SilentPassUI/'
--exclude '.well-known/'
```

**禁止**在守则、运维文档、AI 生成的 SSH 命令里写「只 exclude app/ 与 appTemp/」的旧版片段。

## nginx 约定（真相来源）

`src/x402sdk/service/beamio.conf`：

- `location ^~ /app/` → `try_files $uri $uri/ /app/index.html`
- `location = /app/manifest.json` → Cluster API，静态回退到 `/app/manifest.json`

修改 nginx 或部署流程时，须保证 **`/var/www/beamio.app/app/`** 始终存在且可服务。

## 正确部署方式

使用仓库脚本（推荐）：

```bash
# Homepage → 站点根（自动 exclude app/、appTemp/）
./scripts/deployBeamioHomepage.sh

# SilentPassUI PWA → conet.network remote build → old-conet app/
./scripts/deployBeamioPwa.sh
```

环境变量（可选）：`REMOTE_BUILD_HOST`（默认 `conet.network`）、`REMOTE_BUILD_DIR`（默认 `/var/www/beamio.app/SilentPassUI`）、`REMOTE_APP_DIR`（默认 `/var/www/beamio.app/app/`）。脚本支持 `--skip-version-bump`（不推荐）。

### Homepage（`src/homepage` → 站点根）

**只用** `./scripts/deployBeamioHomepage.sh`（已内置全部 exclude）。若必须手写 rsync，**原样复制**上文「强制 exclude 清单」，不得删减：

```bash
rsync -av --delete \
  --exclude 'app/' \
  --exclude 'appTemp/' \
  --exclude 'pos/' \
  --exclude 'posTemp/' \
  --exclude 'homepage/' \
  --exclude 'SilentPassUI/' \
  --exclude '.well-known/' \
  build/ conet.network:/var/www/beamio.app/
```

服务器上在 `homepage/` 内 `git pull && npm run build` 后，rsync 目标仍是 **`/var/www/beamio.app/` 根**，同样必须带齐全部 exclude（见 `beamio-homepage-deploy-workflow.mdc`）。

### SilentPassUI PWA（独立部署路径）

SilentPassUI 是 Beamio 主 PWA，部署流程与其它 PWA 不同，必须使用以下链路：

1. 本地 `src/SilentPassUI` **working tree clean**（业务改动已 commit）。
2. **`./scripts/deployBeamioPwa.sh` 部署前自动 patch bump** `src/SilentPassUI/package.json` 的 `version`（`npm version patch --no-git-tag-version`），commit + push 后再远程构建。
3. `ssh conet.network`。
4. `cd /var/www/beamio.app/SilentPassUI && git fetch origin cashtree && git reset --hard origin/cashtree && npm install && npm run build`。
5. 将远程 `build/` 发布到 **`/var/www/beamio.app/app/`**（同机本地 rsync；`old-conet` 为 `127.0.1.1` 别名，勿走 SSH 二次 hop）：
   `rsync -av --delete build/ /var/www/beamio.app/app/`
   **目标只能是 `app/`**，禁止对 `/var/www/beamio.app/` 站点根 `--delete`（见上文强制 exclude 清单）。
6. **禁止**用 homepage 的 `rsync --delete build/` 覆盖整个 `/var/www/beamio.app/`，也不要把其它 PWA 的部署目标套用到 SilentPassUI。

## 严禁

- ❌ 对 `/var/www/beamio.app/` 使用 `rsync --delete` 且 **未带齐**「强制 exclude 清单」全部 7 项（含 `homepage/`、`SilentPassUI/`、`.well-known/`）
- ❌ 在 SSH / Agent 命令里「简化」exclude，只写 `app/`、`appTemp/` 的旧示例
- ❌ 把 homepage 或 PWA 的 **服务器 Git 克隆**（`homepage/`、`SilentPassUI/`）当作可 `--delete` 的缓存
- ❌ `rm -rf` 站点根下 `app/`、`appTemp/`、`homepage/`、`SilentPassUI/`、`.well-known/`
- ❌ 用 homepage build 覆盖整站根而不 exclude POS / PWA / Git 克隆
- ❌ 部署 homepage 后未确认 `https://beamio.app/app/`、`/.well-known/apple-app-site-association` 仍正常

## 变更检查清单

- [ ] Homepage 发布是否走 **`deployBeamioHomepage.sh`** 或等价 **7 项 exclude**？
- [ ] 手工 / 服务器内 rsync 是否 **未省略** `homepage/`、`SilentPassUI/`、`.well-known/`？
- [ ] PWA 是否只 **`rsync build/ → app/`**（`deployBeamioPwa.sh`），未对站点根 `--delete`？
- [ ] 部署前 PWA 是否 **bump** `src/SilentPassUI/package.json` 并 push（若走标准 PWA 脚本）？
- [ ] 部署后 spot-check：`https://beamio.app/`、`https://beamio.app/app/`、`/.well-known/apple-app-site-association`？

## 参考

- 部署脚本：`scripts/deployBeamioHomepage.sh`、`scripts/deployBeamioPwa.sh`
- PWA 源码：`src/SilentPassUI/`（`homepage: "/app"`）
- Homepage 源码：`src/homepage/`
- nginx：`src/x402sdk/service/beamio.conf` — `/app/` 路由
- iOS / 深链默认入口：`https://beamio.app/app/`（`BeamioDeepLink.defaultWebAppURL`）
