---
description: Beamio API 服务器必须遵循 cluster/master 协议与链上 pool 队列原则
globs: src/x402sdk/src/{MemberCard.ts,endpoint/**}
alwaysApply: true
---

# Beamio API：Cluster/Master 协议

Beamio 的 API 服务器**必须**遵守 cluster/master 协议。**请勿违反以下约定。**

## 核心原则：预检在 Cluster，Master 信任合格数据

为减少 Master 负荷，**Master 不对任何入站数据再次进行校验**。所有校验必须在 Cluster 阶段完成。

- **Cluster**：负责**所有**预检（格式、必填项、业务校验如 requestHash 过期/已支付、验签、链上 view 只读预检等）。预检通过后才转发。
- **Master**：收到数据时**默认已合格**。Master **不重复做**任何校验，直接入队、执行业务逻辑与链上操作。
- 若将校验逻辑放在 Master（如 `ethers.isAddress`、链上 `getRedeemStatus`、验签），属于**违反协议**，需迁移到 Cluster。

## 架构

- **Cluster (beamioServer, port 2222)**：对外入口，接收请求，做预检
- **Master (beamioMaster, port 1111)**：后端，处理队列、链上操作、DB 写入

## 链上写操作：Pool 队列 + 多钱包并行（nonce 原则）

凡 Master 侧**发送链上交易**的写操作，必须遵守下列执行模型（与 `createCardPoolPress`、`cardRedeemPoolPress` 一致）。

### 两层队列

| 队列 | 含义 | 语义 |
|------|------|------|
| **业务 pool**（如 `cardRedeemPool`、`executeForOwnerPool`） | 待处理 HTTP 请求 | **FIFO**：`shift` 取一条任务 |
| **`Settle_ContractPool`** | factory 登记的 admin 钱包（含 `walletBase`、`baseFactoryPaymaster` 等） | **每任务 shift 一个钱包**上链；用完 `finally` 中 `unshift` 归还 |

### 并行 vs 串行

- **多钱包并行**：每个入队请求可 `kickXxxPoolPress()` 启动 worker；不同 worker 占用 **不同 admin 钱包**，可同时送链上 tx。
- **单钱包串行**：同一 admin 钱包的 nonce 必须递增；同一钱包在同一时刻只应有一个 worker 持有（通过 `shift`/`unshift` 保证，不得并发双持同一 `SC`）。
- **业务 pool 串行 dequeue**：每个 worker 每次只 `shift` 一条任务；任务完成后再 `scheduleXxxPoolPress()` 拉下一条。

### 标准实现模板（Master + MemberCard）

**Master handler（只入队，不预检）：**

```typescript
router.post('/cardRedeem', (req, res) => {
  const { cardAddress, redeemCode, toUserEOA } = req.body as { ... }
  cardRedeemPool.push({ cardAddress, redeemCode, toUserEOA, res })
  kickCardRedeemPoolPress()
})
```

**Cluster handler（完整预检 + 转发规范化 body）：**

```typescript
const preCheck = await cardRedeemPreCheck({ cardAddress, redeemCode, toUserEOA })
if (!preCheck.success) return res.status(403).json({ success: false, error: preCheck.error })
postLocalhost('/api/cardRedeem', {
  cardAddress: resolvedCard,
  redeemCode: String(redeemCode).trim(),
  toUserEOA: ethers.getAddress(toUserEOA),
}, res)
```

**Pool worker（MemberCard.ts）：**

```typescript
export function kickCardRedeemPoolPress(): void {
  cardRedeemPoolPress().catch(/* log */)
}

function scheduleCardRedeemPoolPress(): void {
  if (cardRedeemPool.length === 0) return
  if (Settle_ContractPool.length > 0) kickCardRedeemPoolPress()
  else setTimeout(() => kickCardRedeemPoolPress(), 3000)
}

export const cardRedeemPoolPress = async () => {
  const obj = cardRedeemPool.shift()
  if (!obj) return
  const SC = Settle_ContractPool.shift()
  if (!SC) {
    cardRedeemPool.unshift(obj)
    return setTimeout(() => kickCardRedeemPoolPress(), 3000)
  }
  try {
    // 链上写：factory.redeemForUser / executeForOwner / …
  } finally {
    Settle_ContractPool.unshift(SC) // 仅 finally 归还钱包，禁止 early-return 路径重复 unshift
    scheduleCardRedeemPoolPress()
  }
}
```

### 参考实现

- **多钱包并行 + pool 调度**：`createCardPoolPress`、`cardRedeemPoolPress`（`MemberCard.ts`）
- **Cluster 预检 + Master 只 push**：`/api/cardRedeem`、`/api/cardRedeemPreCheck`（`beamioServer.ts` + `beamioMaster.ts`）

### 严禁（链上写路径）

- ❌ Master 路由内 `ethers.isAddress`、链上 view 预检、验签、业务 reject（应已在 Cluster 完成）
- ❌ Cluster 内直接 `factory.redeemForUser` / `executeForOwner` 等**发链上 tx**
- ❌ 无 pool 队列、在 Master handler 内同步 `await tx.wait()` 阻塞 HTTP（除只读或 webhook 等例外）
- ❌ 同一 `SC` 在 `try` 与 `finally` **重复** `Settle_ContractPool.unshift(SC)`
- ❌ 用 `setInterval` 轮询 pool（须 `kick` + `schedule` + `setTimeout` 链，见 `beamio-no-setinterval.mdc`）

## 写操作

所有**写区块链的 process**必须：

1. Cluster 做**完整预检**（格式、必填项、业务校验）
2. 预检通过后通过 `postLocalhost('/api/xxx', body, res)` **转发到 Master**（body 为规范化字段，勿透传未校验原始 body）
3. Master **只入队**，由 `*PoolPress` worker 通过 **`Settle_ContractPool` 多钱包并行**送链

示例：createCard、purchasingCard、registerSeries、registerMintMetadata、executeForOwner、AAtoEOA、beamioTransferIndexerAccounting、**cardRedeem** 等。

## requestHash 相关预检（Cluster 必须做）

AAtoEOA、beamioTransferIndexerAccounting 等端点，若 body 含 `requestHash`：

- Cluster 必须调用 `GET /api/checkRequestStatus?requestHash=...&validDays=...&payee=...` 校验
- 若 `expired` 或 `fulfilled`，Cluster 返回 403，**不转发**到 Master
- 仅校验通过后才 `postLocalhost` 转发

## 读操作

**读操作**由 Cluster 直接处理，减轻 Master 负担。Master 可选择性实现相同读端点（如 latestCards 两端都有）。

链上 **只读预检**（如 `cardRedeemPreCheck`、`redeemStatusBatch`、`getRedeemStatus`）属于 Cluster 读路径，**不得**在 Master 写路径重复。

## 职责边界（读写分离）

| 类型 | 处理方 | 说明 |
|------|--------|------|
| **读取** | Cluster 直接返回 | 查 DB、查链上 view、RPC 只读等，Cluster 处理并立即返回 |
| **系统写入** | Master 排队处理 | DB 写入（如 registerNfcCardToDb、upsert 等）必须交由 Master，放入 pool 排队 |
| **区块链交易** | Master 排队处理 | 链上写操作必须交由 Master `*Pool` + `Settle_ContractPool` 执行 |

## 需创建钱包时的流程

当预检发现**合法卡但需创建钱包**（如 getUIDAssets 检测到合法 tagID 未绑定 EOA/AA）时：

1. **Cluster**：仅做预检（如 SUN 校验），**成功的卡**才进入下一步
2. **Cluster**：若需创建钱包（DB 写入 + AA 链上创建），**不得**在 Cluster 执行，必须 `postLocalhost` **转发到 Master**
3. **Master**：不再预检，将「创建钱包」放入排队处理 pool，按队列执行
4. **Master**：执行完毕后再做后续读操作（如拉资产）并返回

**严禁**：在 Cluster 内执行 `registerNfcCardToDb`、`provisionOrGetNfcWalletByTagId` 等 DB 写入，或直接调用链上创建 AA。

## 特殊情况

- **coinbase-hooks**：Webhook 需 raw body 验签，Cluster 与 Master 均可独立接收处理，不转发（转发会丢失 raw body）

## x402sdk API 修改后的发布流程

修改 `src/x402sdk` 中 API、OG、endpoint、服务端构建产物相关代码后，必须按独立子项目流程发布：

1. **本地验证**：在 `src/x402sdk` 内执行该子项目自己的验证命令，至少 `npm run build`。
2. **子项目提交与推送**：
   - `cd src/x402sdk`
   - `git status -sb` 确认只包含本次任务相关改动。
   - `git add <changed-files> && git commit -m ... && git push`
3. **根仓同步**：
   - 回到根仓，`git add src/x402sdk` 更新 submodule 指针。
   - 若本次同时更新 `.cursor/rules`、部署脚本或根仓配置，一并纳入根仓 commit。
   - `git commit ... && git push`。
4. **远程部署**：
   - `ssh conet.network`
   - 进入服务器上的 `x402sdk` 工作目录，执行 `git pull`。
   - 执行 `npm run build`。
   - 重启 Beamio API systemd 服务（按服务器实际服务名，如 `systemctl restart conet-beamio-api`）。
   - 用 `systemctl status ...` 或接口 smoke test 确认 API 已运行。

注意：只 `git pull` 不代表运行中的 `dist/server.js` 已更新；必须 build 并重启 API。

## HTTP 响应边界（EntryPoint 确认即返回）

经 EntryPoint / UserOp relay 的商户写操作：**Cluster 预检 B-Unit 余额（只读）**；Master 在 **业务 tx receipt 确认** 后立即 `res.json`，**不得**阻塞等待 CoNET B-Unit 扣款或 indexer 记账。详见 **`beamio-entrypoint-confirmed-return-protocol.mdc`**。

## 新增端点检查清单

新增 API 时：

- [ ] 若是**写操作**：Master 添加 **只 push pool + kick worker** 的 handler；Cluster 添加**完整预检** + `postLocalhost` 转发规范化 body
- [ ] 若是**读操作**：在 Cluster 添加 handler，Master 建议同步实现（便于直连 Master 或容错）
- [ ] **不要**仅在 Cluster 实现写操作的完整链上逻辑
- [ ] **不要**在 Master 做任何入站数据校验（格式、业务、验签、链上 view 预检），全部在 Cluster 完成
- [ ] 链上写是否使用 `xxxPool` + `*PoolPress` + `Settle_ContractPool` shift/unshift？
- [ ] worker 结束是否 `scheduleXxxPoolPress()`（有空闲钱包立即续跑，否则 delayed kick）？
- [ ] 若读操作中**可能需创建钱包**（DB 写入 + AA 链上创建）：Cluster 仅预检，需创建时转发 Master 排队处理，**不得**在 Cluster 内执行 provision/registerNfcCardToDb
