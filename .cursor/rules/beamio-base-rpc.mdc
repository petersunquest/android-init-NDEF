---
description: Beamio Base RPC standard - project default for Base chain
alwaysApply: true
---

# Beamio Base RPC 标准

**项目内 Base 链 RPC 统一使用 CoNET 官方 Base RPC（`base-rpc.conet.network`）。**

## 端点

- **HTTP**: `https://base-rpc.conet.network`
- **WebSocket**: `wss://base-rpc.conet.network/ws`

## 适用范围

- Hardhat / 部署脚本：`BASE_RPC_URL`
- CoNET-SI vote 轮询、/base-rpc 转发：优先 `BASE_RPC` (wss)，否则 `BASE_RPC_HTTP`
- x402sdk / MemberCard：`base_endpoint`（~/.master.json）或默认
- 各类脚本、诊断工具：`BASE_RPC`、`BASE_RPC_URL`

## 配置

- `.env`：`BASE_RPC_URL=https://base-rpc.conet.network`
- `~/.master.json`：`base_endpoint: "https://base-rpc.conet.network"`
- CoNET-SI：优先 `BASE_RPC=wss://base-rpc.conet.network/ws`，否则 `BASE_RPC_HTTP=https://base-rpc.conet.network`

## 新增代码

新增 Base 链 RPC 调用时，默认使用 `https://base-rpc.conet.network`。

## 运维与重启（Agent 须遵守）

**不得** 未经用户在同一条消息里 **明确书面授权** 而停止、重启、 wipe 或跑快照重置 **`base-rpc.conet.network` 背后的 Base 链基础设施**（**全部** `op-reth` / `op-geth` / `op-node` 实例及 `docker compose` / `base-node-reset-from-snapshot.sh` 等）。详见 **`critical-chain-infra-restart-requires-approval.mdc`**（与 CoNET **全部** beacon / validator 规则同一标准）。

- RPC 502、sync 落后、限流 → 只读诊断 + 向用户说明；**不** 自动 `docker restart base-op-reth`。
- 仅 nginx / CoNET-SI `/base-rpc` 反代 reload **不** 等同 Base 节点重启。
