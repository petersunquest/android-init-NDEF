---
description: Trusted caching rules for all Beamio RPC/on-chain fetch flows
alwaysApply: true
---

# Beamio AI 链上拉取与可信缓存守则

所有通过 RPC / 链上接口拉取数据的流程，都必须遵守“**Daemon 先读本地可信缓存、后台链上刷新**”原则。拉取成功为可信任数据，拉取失败为非可信数据。

## 全局架构优先级

- **Daemon 是链上读取的唯一执行层**：负责 target 注册、RPC 调用、解析、TTL/in-flight 去重、串行化、trusted cache hydrate/write，以及向全局 context mirror 发布结果。
- **页面和组件只读 Daemon mirror**：不得在 render、页面 `useEffect` 或组件 hook 中自行 RPC/fetch 同一链上维度，也不得自行维护该维度的长期缓存。
- 页面只能注册查询目标或触发 Daemon refresh；注册 API 不得要求页面传入执行 RPC 的 callback。旧 callback 注册接口仅作迁移兼容，新增指标不得继续扩展该模式。

## Core Rule

- **以 Daemon 的本地 hydrate mirror 为主要依据表示**：首屏优先使用 Daemon 从本地缓存、本地 state、profile 缓存或持久化缓存 hydrate 的结果。
- **后台异步拉取**：由 Daemon 发起链上 RPC 拉取最新数据。
- **拉取成功（可信任数据）**：Daemon 才允许覆盖 mirror、本地缓存并通知 UI。
- **拉取失败（非可信数据）**：Daemon 必须继续提供“最后一次成功获取的可信数据”，不得用失败结果覆盖 mirror/本地。

## Cache & Display Logic

| 本次拉取结果 | 本地缓存 | 显示依据 | 是否更新本地 |
|-------------|---------|---------|-------------|
| 成功（可信任） | 有/无 | 本次拉取结果 | ✅ 写入/更新 |
| 失败（非可信） | 有 | 本地缓存为主 | ❌ 不覆盖 |
| 失败（非可信） | 无 | loading / unavailable | ❌ 无数据可写 |

## What Counts As Failure (非可信数据)

- RPC 超时、连接失败、网络中断、429、5xx、provider 限流、解析失败、返回结构异常。
- 任何无法确认“本次结果真实可信”的场景，都按失败处理，而不是按空数据处理。

## What Counts As Trusted Success (可信任数据)

- 请求成功完成，返回结构符合预期。
- 数据来源与当前查询对象匹配，不能串账号、串卡、串链。
- 对“空结果”的解释必须有明确依据；只有**成功且明确返回空**时，才允许清空缓存或显示空态。

## Mandatory Behavior

- 不得把“请求失败”解释为“链上数值为 0”。
- 不得把“请求失败”解释为“用户没有资产 / 没有记录 / 没有卡 / 没有余额”。
- 不得在失败时用占位符覆盖已存在的可信数据显示。
- 必须按用户、卡地址、链 ID、查询维度做缓存隔离，避免串数据。
- **必须按 EOA 隔离用户级缓存**：四个指标面板、transactions、protocol fuel 等与当前登录 EOA 相关的数据，cache key 必须含 EOA；用户切换 EOA 登录后，不得显示上一 EOA 的缓存信息。
- 必须防止旧请求覆盖新请求；多次并发拉取时，仅最新请求可写入 state。

## UI Rendering Rule

- 有最后一次可信缓存时：显示缓存；若最近一次拉取失败，可标记“本地信息为主”或“stale / last updated ...”。
- 无任何可信缓存，且首次请求仍未成功时：才允许显示 loading / unavailable。
- 一旦拿到可信成功结果，立即刷新 UI 并更新本地缓存。

## Immutable History Rule

- 对交易、流水、活动历史、账本记录，不得因一次拉取失败而删除旧记录。
- 仅在链上成功校验后，才允许清空、删除或负向覆盖本地历史。

## Trusted Cache Protocol（链上可信信息本地缓存）

当获得**链上可信信息**（如 admin 状态、权限标志等）后，必须按以下协议缓存与展示：

### 原则

- **本地为主、链上为辅**：下次登录时优先用本地缓存快速显示/隐藏，再后台拉取链上最新数据。
- **仅成功时写入**：只有链上拉取成功时才写入 trusted cache；失败时不得覆盖已有缓存。
- **EOA 隔离**：cache key 必须含 EOA，避免跨用户串数据。

### 流程

1. **Daemon 首屏**：按完整 query key 读取 trusted cache，立即 hydrate 对应全局 mirror。
2. **Daemon 后台**：异步发起链上 RPC；同一 key 只允许一个 in-flight 请求。
3. **可信成功**：Daemon 更新 mirror 并 `saveTrustedCache(key, value)`，页面自动获得新值。
4. **不可信失败**：Daemon 不覆盖 mirror；有缓存则继续提供缓存，无缓存才显示安全默认。

### 示例（admin 状态）

```
cacheKey = `eoa:${currentEoa}:card:${cardAddress}:is-admin`
// 1. 先读 loadTrustedCache(cacheKey)，有则 setState 立即显示
// 2. fetchWithCache 拉链上 card.isAdmin(addr)
// 3. 成功 → setState + saveTrustedCache(cacheKey, ok)
// 4. 失败 → 若有 cache 不覆盖；无 cache 则 setState(false)
```

### 严禁

- ❌ 链上拉取失败时写入或覆盖 trusted cache。
- ❌ 用户级 trusted cache 的 key 不含 EOA。
- ❌ 忽略本地缓存，每次登录都等链上拉取完成才显示。

## AI Execution Checklist

- [ ] 是否由 Daemon 先 hydrate 本地缓存，再发起 RPC 刷新。
- [ ] UI 是否只读取 Daemon 全局 mirror，没有页面内 RPC/fetch？
- [ ] 是否只有在“成功且可信”时才写回缓存与 UI。
- [ ] 拉取失败时是否继续显示本地缓存，并可选标记“本地信息为主”。
- [ ] 是否在 RPC 故障时继续显示最后一次可信数据，而不是显示 `—`。
- [ ] 是否区分“请求失败”和“成功返回空”这两种完全不同的语义。
- [ ] 是否加入请求序号、取消标记或等价机制，避免旧请求回写。
- [ ] **用户级数据 cache key 是否含 EOA？** 切换 EOA 登录后，不得显示旧 EOA 的缓存数据。
- [ ] **链上可信信息**是否按 Trusted Cache Protocol 实现（本地为主、仅成功写入、EOA 隔离）？
