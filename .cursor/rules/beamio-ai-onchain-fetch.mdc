---
description: Trusted caching rules for all Beamio RPC/on-chain fetch flows
alwaysApply: true
---

# Beamio AI 链上拉取与可信缓存守则

所有通过 RPC / 链上接口拉取数据的流程，都必须遵守“**最后一次成功且可信的数据优先**”原则。

## Core Rule

- 首屏优先使用本地缓存、本地 state、profile 缓存或持久化缓存渲染。
- 后台再异步拉取链上最新数据。
- 只有当本次拉取**成功**，且返回结果**可判定为可信**时，才允许覆盖本地缓存与当前 UI。
- 若本次拉取因 RPC 通讯故障失败，必须继续使用“最后一次成功获取的可信数据”，**不得**显示 `-`、`—`、`0`、空数组或“无数据”来覆盖旧值。

## What Counts As Failure

- RPC 超时、连接失败、网络中断、429、5xx、provider 限流、解析失败、返回结构异常。
- 任何无法确认“本次结果真实可信”的场景，都按失败处理，而不是按空数据处理。

## What Counts As Trusted Success

- 请求成功完成，返回结构符合预期。
- 数据来源与当前查询对象匹配，不能串账号、串卡、串链。
- 对“空结果”的解释必须有明确依据；只有**成功且明确返回空**时，才允许清空缓存或显示空态。

## Mandatory Behavior

- 不得把“请求失败”解释为“链上数值为 0”。
- 不得把“请求失败”解释为“用户没有资产 / 没有记录 / 没有卡 / 没有余额”。
- 不得在失败时用占位符覆盖已存在的可信数据显示。
- 必须按用户、卡地址、链 ID、查询维度做缓存隔离，避免串数据。
- 必须防止旧请求覆盖新请求；多次并发拉取时，仅最新请求可写入 state。

## UI Rendering Rule

- 有最后一次可信缓存时：显示缓存，并可标记为“stale / last updated ...”。
- 无任何可信缓存，且首次请求仍未成功时：才允许显示 loading / unavailable。
- 一旦拿到可信成功结果，立即刷新 UI 与本地缓存。

## Immutable History Rule

- 对交易、流水、活动历史、账本记录，不得因一次拉取失败而删除旧记录。
- 仅在链上成功校验后，才允许清空、删除或负向覆盖本地历史。

## AI Execution Checklist

- [ ] 是否先读本地可信缓存，再发起 RPC 刷新。
- [ ] 是否只有在“成功且可信”时才写回缓存与 UI。
- [ ] 是否在 RPC 故障时继续显示最后一次可信数据，而不是显示 `—`。
- [ ] 是否区分“请求失败”和“成功返回空”这两种完全不同的语义。
- [ ] 是否加入请求序号、取消标记或等价机制，避免旧请求回写。
