# Beamio AI 链上数据拉取守则

本守则约束 Beamio AI 在处理链上数据（尤其是 Fuel Center / B-Units Ledger）时的拉取、缓存与渲染策略。

## 1) “访问失败”绝不等于“零数据”

- 链上访问失败（RPC 超时、网络错误、服务不可达、429/5xx、解析失败）时，**不得**将结果解释为 `[]` 或 `0`。
- 失败场景下，必须保留现有缓存数据，并将状态视为“本次刷新失败”（stale but usable）。
- 只有在**链上访问成功**且返回明确空结果时，才可信任“无数据”并清空对应缓存/UI。

## 2) 交易记录不可变（Transaction Is Permanent）

- Transaction 视为永久审计记录，不允许因一次请求失败而删除本地已存在记录。
- 删除或置空记录的前提必须是：**成功读取链上后确认该记录不存在**。
- 对于非成功响应（错误/超时/中断），只能保留并等待下一次成功校验，不得做负向覆盖。

## 3) 缓存优先渲染（低迟滞）

- 打开页面时先使用本地缓存完成首屏渲染，避免 UI 卡顿与空白。
- 后台异步拉取链上最新数据；成功后再做差量更新。
- 若链上返回失败：继续显示缓存，不触发“无记录”态。

## 4) Items 拉取与渲染优先级

- 优先级顺序：
  1. 新入库 items（最新时间戳/最新区块）
  2. 当前视口渲染必须 items（首屏可见与交互关键项）
  3. 历史非可见 items
- 首屏只需最小必要批次，保证交互先可用，再补全历史数据。

## 5) 后台补全策略（小批量、长间隔）

- 对非可见历史 items 使用小批量拉取与合并（如 10~30 条/批）。
- 批次间隔应长于首屏渲染节奏（如 100~500ms），避免抢占主线程。
- 每批完成后再进入下一批，支持中断与重入（用户切页/账号切换时终止旧任务）。

## 6) 一致性判定准则

- 成功返回空：可信空数据，可清空。
- 成功返回非空：按链上为准更新缓存与 UI。
- 访问失败：维持旧缓存，不得清空。

## 7) Fuel Center / B-Units Ledger 额外要求

- Smart Receipt 优先展示原始 Transaction（rawTx）。
- 若 rawTx 缺失且链上访问失败：保留旧记录并标记为待校验，不得直接删除。
- 仅在链上成功校验后，才允许对旧记录进行删除或置空。

## 8) Recent Activity（Home）链上历史更新协议（参考 Fuel Center）

- **新数据优先（New-data-first）**：链上历史默认不可变，刷新时先判断是否出现“新交易 ID / 新 txHash”。
- **无新数据不重绘**：若没有新链上记录，必须复用现有 state 引用，不得整表替换（避免 UI 抖动）。
- **仅增量合并**：检测到新记录时，只将新记录合并到现有列表；已有记录保持对象引用不变。
- **稳定 key**：列表 key 必须使用稳定主键（优先 txHash / id），禁止使用时间戳等易波动字段作为主键。
- **并发防抖（fetch sequence）**：多次刷新并发时，仅允许最新请求写入 state；旧请求结果必须丢弃。
- **失败回退策略**：链上请求失败时保留旧列表，不得清空或回退为“无记录”。

## 9) AI 编程执行清单（强制）

- [ ] 是否先用缓存/旧 state 快速渲染首屏，再后台拉链上最新。
- [ ] 是否按“新数据优先”判断，仅在出现新记录时更新 state。
- [ ] 是否保证 key 稳定且不包含波动字段（如格式化后的时间）。
- [ ] 是否加入 fetchSeq / request token，避免旧请求覆盖新请求。
- [ ] 是否在失败场景保留历史，不把错误当空数据。

## 10) History 用户卡片（Owned Cards）本地优先协议

- `/history` 的用户卡片（My BeamioUserCards）必须采用“本地缓存优先 + 后台有效更新”。
- 进入页面时：
  1. 先从本地（`CoNET_Data` / profile 缓存）读取并渲染，避免首屏空白。
  2. 后台异步拉取链上/后端数据。
- 仅当后端返回“有效数据”（trusted source）时，才回写本地缓存并覆盖 UI。
- 拉取失败时保留本地已显示卡片，不得清空为“无卡”。
- 缓存写回需按用户维度隔离（至少 EOA/AA 组合键），避免不同账号串数据。
