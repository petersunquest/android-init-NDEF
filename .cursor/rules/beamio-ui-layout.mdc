---
description: Beamio 项目 UI 主设置 protocol：页首内容与顶部间距
globs: src/SilentPassUI/**/*.{tsx,ts,jsx,js}
alwaysApply: false
---

# Beamio UI 布局 Protocol

## 页首内容与顶部间距

Beamio 各页面的**首个显示控件与顶部**的间距必须统一，使用以下 protocol：

```html
<!-- 顶部留白：刘海 + 5rem，统一各页首内容距顶距离 -->
<div className="shrink-0" style={{ minHeight: 'calc(env(safe-area-inset-top) + 5rem)' }} />
```

该 spacer 应放在滚动容器的**第一个子元素**，确保首屏内容（如 /home 的 Total Valuation、/chat 的 ChatList、All Activity 的胶囊等）与顶部的距离一致。

### 使用示例

- `/home`：在 flex-1 overflow-y-auto 内，spacer 后接 Total Valuation 等
- `/chat`：在 flex-1 overflow-y-auto 内，spacer 后接 ChatList
- `MyWalletDashboardNew`：同上
- `Market`：同上
- **All Activity 全屏页**：在 px-4 的滚动 div 内，spacer 后接 ActiveHistoryPannelNew（胶囊为首个控件）
- **Fuel Center (FuelView)**：全屏 overlay，根容器加 `pl-[env(safe-area-inset-left)] pr-[env(safe-area-inset-right)] pb-[calc(8rem+env(safe-area-inset-bottom))]`。两种顶部间距：
  - **返回按钮 + title**：对齐 Home 胶囊，`paddingTop: 'max(1rem, env(safe-area-inset-top))'`
  - **首内容**（Network Fuel Balance）：对齐 Home 首内容，spacer `minHeight: calc(env(safe-area-inset-top) + 5rem)` + 内容区 `pt-6`

## B-Units 视觉标准（全局）

涉及 `B-Units` 数值与 `Fuel` 图标的 UI（尤其是 `/home` Recent Activity detail、`/History`、Fuel Center）必须使用 Beamio 标准橙色，不得用灰色/蓝色替代：

- 图标：`text-orange-500`
- 数值文字：`text-orange-500`
- 胶囊底与边框：`bg-orange-50 border-orange-200`（暗色模式用 `dark:bg-orange-900/20 dark:border-orange-800/50`）

若同一行展示的是 B-Units（例如 `Network Gas`、`Beamio Fee` 右侧），颜色需与 Fuel Center 的 B-Units 表达保持一致。

## B-Units 数值格式标准（全局）

Beamio 项目中所有面向用户显示的 `B-Units` 金额，统一使用 **2 位小数**：

- 示例：`2.00 B-Units`、`-1.50 B-Units`、`+100.00 B-Units`
- 适用范围：`/home` Recent Activity、detail 面板、Fuel Center、History 相关明细、所有新 UI
- 禁止混用整数与不同小数位（除非明确是开发调试日志，不对用户展示）
