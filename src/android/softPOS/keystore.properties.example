# Release signing uses the same keystore as android-NDEF / Android-init-NDEF.
# Source of truth: ../../Android-init-NDEF/keystore.properties (not committed).
# Example: ../../Android-init-NDEF/keystore.properties.example

storePassword=
keyPassword=
keyAlias=beamio-app
storeFile=beamio-app-release.keystore
