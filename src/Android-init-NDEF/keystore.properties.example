# Copy to keystore.properties and fill in values. Never commit keystore.properties.
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=beamio-app
storeFile=beamio-app-release.keystore
